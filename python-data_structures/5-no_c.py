#!/usr/bin/python3
def no_c(my_string):
    new_string = ""
    #Browse my_string
    for char in my_string:
    #my_string contain specific character ? (c C)
        # Yes ==> Remove
        else:
            return new_string
